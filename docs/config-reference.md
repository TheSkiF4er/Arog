# Конфигурация Farog (`farog.toml`)

Основные поля:

- `name` — имя проекта;
- `version` — версия;
- `entry` — точка входа;
- `type` — тип модулей (например, `module`).

Секции:

- `[permissions]` — разрешения (network, fs и др.);
- `[build]` — настройки сборки.
